services:
  franquias-api:
    build: .
    container_name: franquias-api
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    deploy:
      resources:
        limits:
          cpus: "0.8"
          memory: 1536M
